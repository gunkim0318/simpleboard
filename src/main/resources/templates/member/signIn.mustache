<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>로그인</title>
    <link rel="stylesheet" href="/css/mui.min.css"/>
</head>
<body>
    <link rel="stylesheet" href="/css/member/signIn.css"/>
    <div class="mui-panel frame">
        <form class="mui-form" action="/member/signIn" method="post">
            <legend><h1 style="text-align: center;">로그인</h1></legend>
            <input name="_csrf" type="hidden" value="{{_csrf.token}}" />
            <div class="mui-textfield">
                <input type="text" name="email" placeholder="아이디를 입력하세요.">
            </div>
            <div class="mui-textfield">
                <input type="text" name="password" placeholder="비밀번호를 입력하세요.">
            </div>
            <div>
                <button id="signInBtn" class="mui-btn mui-btn--raised mui-btn--primary" style="width: 100%;">로그인</button>
                <hr/>
                <button id="signUpBtn" class="mui-btn mui-btn--raised" style="width: 100%;">회원가입</button>
            </div>
        </form>
    </div>
{{>layout/footer}} <!--</body>-->
<script src="/js/member/signIn.js"></script>
